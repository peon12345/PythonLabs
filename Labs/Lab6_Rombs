import pygame
import Colors
pygame.init()


size_diamond = int(input())

w = 155
h = 155

screen = pygame.display.set_mode((w,h))
screen.fill(Colors.YELOW)

n_diamond_row = int(w/size_diamond)
n_diamond_col = int(h/size_diamond)

for row in range(n_diamond_row):
    for col in range(n_diamond_col):
        